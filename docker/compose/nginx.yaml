version: '2'
services:
  nginx:
    container_name: indow_nginx
    image: nginx
    network_mode: "host"
    volumes:
      - ../nginx/default_local:/etc/nginx/conf.d/default.conf
      - ../nginx/proxy_params:/etc/nginx/proxy_params
    ports:
      - 80:80
